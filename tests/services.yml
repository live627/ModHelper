parameters:
    my.hooks:
        - [Foo, /vendor/foo]
        - [BarDoom, /vendor/foo.bardoom]

services:
    collection:
        class:     ModHelper\Collection
    linktree:
        class:     ModHelper\Linktree
        arguments: ["@collection"]
    hooks:
        class:     ModHelper\Hooks
        arguments: ["@collection","%my.hooks%"]
